{
  "installCommand": "npm install && npm install @rollup/rollup-linux-x64-gnu --no-save",
  "buildCommand": "npm run build -w client",
  "outputDirectory": "client/dist",
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}